from django.contrib import admin
# from .models import WelderPerformanceRecord

# Register your models here.
# Note: Django admin doesn't work well with MongoEngine models
# admin.site.register(WelderPerformanceRecord)