from django.contrib import admin
# from .models import WelderCertificate

# Register your models here.
# Note: Django admin doesn't work well with MongoEngine models
# admin.site.register(WelderCertificate)